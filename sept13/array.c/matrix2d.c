#include <stdio.h>
int main()
{
    int rows;
    int cols;
    printf("Enter a number of rows: ");
    scanf("%d", &rows);  
    printf("Enter a number of cols: ");
    scanf("%d", &cols);  
    for()
    return 0;
}
